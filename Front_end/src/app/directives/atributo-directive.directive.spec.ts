import { AtributoDirectiveDirective } from './atributo-directive.directive';

describe('AtributoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AtributoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
